const config = {
    dev: process.env.NODE_ENV !== 'production',
    production: process.env.NODE_ENV === 'production',
    dialogFlowAccessToken: '5ede9a14ce884c3894f9c52a1cf190111'
}

export default config;
